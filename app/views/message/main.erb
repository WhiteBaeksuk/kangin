<div class="well" id="menu_font" style="font-size: 20px;" >
  <center>받은 메시지와 보낸메시지 관리 페이지입니다.<br>
  (아래의 쪽지보내기 버튼을 이용하여 메시지 작성이 가능합니다)<br>
  <a href="/message/write" class="btn btn-warning" id="menu_font" style="font-size: 20px;" >쪽지보내기</a></center>
</div>
<br>

<div class="container">
  <div class="row">
            <div class= "col-md-12">
              <div class="panel panel-info">
               
                <div class="panel-heading">
                  <div class="panel-title" style="font-size: 20px;" id="menu_font"><center>받은 메시지</center></div>
                </div>
               
                <div class="panel-body">
                    <table class="table table-striped table-hover ">
                        <thead>
                          <tr style="font-size: 18px;" id="menu_font_t">
                            <th>#</th>
                            <th>보낸 사람</th>
                            <th>보낸 날짜 및 시간</th>
                            <th>내용 미리보기</th>
                       </tr>
                       </thead>
                       
                        <tbody>
                        
                          <% @receive_message.reverse.each do |m|%> <!--유저 디비를 생성하면 유저별로 메시지 출력해야되니까 user.messages.each do 하고 걸러줄것-->
                          <tr>
                           <td style="font-size: 18px;" id="menu_font_t"><%=m.id%></td>
                           <td style=" font-size: 18px;" id="menu_font_t"><%=User.find(m.sender).student_name%></td>
                           <td style="font-size: 18px;" id="menu_font_t"><%=m.created_at%></td>
                           <td style="font-size: 18px;" id="menu_font_t"><%=m.content%></td>
                           <td><a href="/message/read/<%=m.id%>" class="btn btn-primary btn-sm" style="text-align: center; font-size: 18px;" id="menu_font">쪽지보기</a>
                               <a href="/message/reply/<%=m.id%>" class="btn btn-primary btn-sm" style="text-align: center; font-size: 18px;" id="menu_font">답장보내기</a></td> <!--답장보낼때는 보낸이랑 받는이 뒤집에서 다시 보내기 수정할것--> 
                          </tr> 
                          <% end %>
                          
                        </tbody>
                     </table>
                     
                </div>
                
              </div>
            </div>      
            
   <div class= "col-md-12">
    <div class="panel panel-info">
      <div class="panel-heading">
         <div class="panel-title" style="text-align: center; font-size: 20px;" id="menu_font"><center>보낸 메시지</center></div>
       </div>
      <div class="panel-body">
        <table class="table table-striped table-hover ">
          <thead>
            <tr style="font-size: 18px;" id="menu_font_t">
              <th>#</th>
              <th>받은 사람</th>
              <th>보낸 날짜 및 시간</th>
              <th>내용 미리보기</th>
            </tr>
          </thead>
          <tbody>
          <% @send_message.reverse.each do |m|%> <!--유저 디비를 생성하면 유저별로 메시지 출력해야되니까 user.messages.each do 하고 걸러줄것-->
          <tr>
            <td style="font-size: 18px;" id="menu_font_t"><%=m.id%></td>
            <td style="font-size: 18px;" id="menu_font_t"><%=User.find(m.receiver).student_name%></td>
            <td style="font-size: 18px;" id="menu_font_t"><%=m.created_at%></td>
            <td style="font-size: 18px;" id="menu_font_t"><%=m.content%></td>
            <td><a href="/message/read/<%=m.id%>" class="btn btn-primary btn-sm" style="text-align: center; font-size: 18px;" id="menu_font">쪽지보기</a></td>
          </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
</div>

</div>
  </div>  


<br><br>

