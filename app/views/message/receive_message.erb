<div class="col-md-6">
    <div class="panel panel-info">
      <div class="panel-heading">
        <h3 class="panel-title"><center>받은 메시지</center></h3>
      </div>
      <div class="panel-body">
          <table class="table table-striped table-hover ">
              <thead>
                <tr>
                  <th>#</th>
                  <th>보낸 사람</th>
                  <th>보낸 날짜</th>
                  <th>내용 미리보기</th>
                </tr>
              </thead>
              
              <tbody>
                
                <% @receive_message.reverse.each do |m|%> <!--유저 디비를 생성하면 유저별로 메시지 출력해야되니까 user.messages.each do 하고 걸러줄것-->
                <tr>
                  <td><%=m.id%></td>
                  <td><%=m.sender%></td>
                  <td><%=m.created_at%></td>
                  <td><%=m.content%></td>
                  <td><a href="/message/read/<%=m.id%>" class="btn btn-default">쪽지보기</a>
                      <!--<a href="/message/reply/<2%=m.id%>" class="btn btn-default">답장보내기</a></td> 답장보낼때는 보낸이랑 받는이 뒤집에서 다시 보내기 수정할것-->
                </tr>
                <% end %>
                
              </tbody>
            </table>
           
      </div>
    </div>
</div>